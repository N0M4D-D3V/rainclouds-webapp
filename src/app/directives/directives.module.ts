import { NgModule } from '@angular/core';
import { LazyImageLoadingDirective } from './lazy-img.directive';

@NgModule({
  imports: [],
  declarations: [LazyImageLoadingDirective],
  exports: [LazyImageLoadingDirective],
})
export class DirectivesModule {}
